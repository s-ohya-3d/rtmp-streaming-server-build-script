# rtmp-streaming-server build script
A rtmp streaming server build script for nginx/tengine/openResty.

# System requirement:

1. Debian/Ubuntu
2. CentOS
3. freeBSD

# Usage:

```
sudo bash ./build.sh
```
